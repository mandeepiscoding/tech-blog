{{#each postData as |post|}}
{{> post-details}}
{{/each}}
{{#if isDashboard}}
  <a href="./newpost">
    <button class="btn btn-primary" type="button" id="new-post-btn" >New Post</button>
  </a>
{{/if}}